import 'package:flutter/material.dart';

class AnimalChatApp extends StatelessWidget {
  const AnimalChatApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
